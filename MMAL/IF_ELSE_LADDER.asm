MOV AX,0H ;GRADE
MOV BX,68H ;GETMARKS

MOV CX,80H
CMP BX,CX
JA A

MOV CX,70H
CMP BX,CX
JA B

MOV CX,60H
CMP BX,CX
JA C

MOV CX,50H
CMP BX,CX
JA D

A:
    MOV AX,0AH
    JMP EXT
B:
    MOV AX,0BH
    JMP EXT
C:
    MOV AX,0CH
    JMP EXT
D:
    MOV AX,0DH
    JMP EXT
EXT:
    RET
